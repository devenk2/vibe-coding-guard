# Secrets — never commit
.env
.env.*
!.env.example

# Runtime output from hook scans
audit.log

# Claude Code user-local overrides
.claude/settings.local.json

# macOS
.DS_Store

# Editor / backup
*.bak
*.swp
*~
