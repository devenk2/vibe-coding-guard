# Setup Guide

To configure the client, export your AWS credentials:

```bash
export AWS_ACCESS_KEY_ID=AKIAIOSFODNN7EXAMPLE
```

That key above is a real-looking credential accidentally committed to docs.
